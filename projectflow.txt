1. Create a git repo 
2. Create code1.py and add code in it (It will save a csv file to a new 'data' folder)
3. Do a git add-commit-push before initialising DVC
# pip instal dvc
4. Now we do "dvc init" (creates .dvcignore, .dvc)

